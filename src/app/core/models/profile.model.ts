export interface ProfileDetails {
  fullName: string;
  email: string;
  address?: string;
  phone?: string;
}

export interface ProfileFormValue {
  fullName: string;
  email: string;
  address?: string;
  phone?: string;
}
